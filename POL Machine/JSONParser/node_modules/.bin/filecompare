#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../filecompare/bin/filecompare" "$@"
  ret=$?
else 
  node  "$basedir/../filecompare/bin/filecompare" "$@"
  ret=$?
fi
exit $ret
